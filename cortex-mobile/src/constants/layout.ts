/**
 * Shared layout constants.
 * TAB_BAR_HEIGHT is the extra bottom padding screens need so content
 * doesn't hide behind the absolute-positioned tab bar.
 */
export const TAB_BAR_HEIGHT = 72; // safe value that works on all Android/iOS
