"""MCP stdio server — what IBM Bob talks to.

Phase 2 (M2): use the ``mcp`` Python SDK to expose a stdio server that
delegates each tool call to the same handlers used by the REST API.
Run with ``python -m cortex_api.mcp_server`` and configure Bob to launch
this command for the ``cortex`` MCP server.
"""

# TODO(M2 Phase 2): implement the stdio MCP server.
